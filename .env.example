PORT=5000
# Example local MongoDB (DO NOT commit real secrets here)
MONGO_URI=mongodb://127.0.0.1:27017/ecommerce
NODE_ENV=production
JWT_SECRET=your_jwt_secret_here
# Frontend URL - update with your Vercel domain
FRONTEND_URL=https://mern-ecommerce-tushank99s-projects.vercel.app/

# Notes:
# - Copy this file to `.env` locally before running scripts that require MONGO_URI.
# - Do NOT commit real `.env` secrets. Use Vercel dashboard to set production env vars.